{
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /trainings/ --out ../../dist/trainings",
    "export": "slidev export --per-slide --output ../trainings.pdf"
  }
}
