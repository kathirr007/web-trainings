{
  "name": "trainings",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "main": "main.ts",
  "scripts": {
    "dev": "vite",
    "build-vite": "vue-tsc -b && vite build",
    "build": "vite build --base /trainings/ --outDir ../dist/trainings",
    "preview": "vite preview --base /trainings/ --outDir ../dist/trainings"
  }
}
